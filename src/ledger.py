#!/usr/bin/env python3


class Ledger:
    def __init__(self):
        ''' 
        - check if ledger exist
            - if not exist:
                - create it
            - if exist:
                - check if corrupt
        '''
